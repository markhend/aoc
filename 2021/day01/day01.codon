with open('input.txt', 'r') as f:
    inp = [int(line.strip()) for line in f]
    cnt = 0
    for curr, next in zip(inp[:-1], inp[1:]):
        if next > curr:
            cnt += 1
    print(cnt)
    

